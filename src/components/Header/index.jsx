import React from 'react';
import {
  Navbar,
} from 'reactstrap';

import './index.scss';

const Header = () => (
  <Navbar className="header">
    <a href="/">PIXEL CHART</a>
  </Navbar>
);

export default Header;